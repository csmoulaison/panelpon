#include "cursor_common.h"

#include "board.h"

bool cur_common_can_flip(struct Game* game, uint8_t* positions, uint8_t positions_len) {
    (void)game;
    (void)positions;
    (void)positions_len;
    return true;
}
